<!-- views/index.ejs -->
<!doctype html>
<html>

<head>
    <% include partials/head %>
    <script src='https://www.google.com/recaptcha/api.js'></script>
</head>

<body>
    <header>
        <% include partials/header %>
    </header>
    <main>

        <div class="container">

            <div class="col-sm-6 col-sm-offset-3">

                <h1><span class="fa fa-sign-in"></span> Crea tu cuenta</h1>

                <!-- show any messages that come back with authentication -->
                <% if (message.length > 0) { %>
                    <div class="alert alert-danger">
                        <%= message %>
                    </div>
                    <% } %>

                        <!-- LOGIN FORM -->
                        <form action="/signup" method="post">
                            <div class="form-group">
                                <label>Email</label>
                                <input type="text" class="form-control" name="email">
                            </div>
                            <div class="form-group">
                                <label>Password</label>
                                <input type="password" class="form-control" name="password">
                            </div>
                            <!-- usaremos recaptcha -->
                          <!--  <div class="form-group">
                                <label>Ingresa los números que aparecen en la siguiente imagen:</label> <br>
                                <button type="button" name="reload" onclick="cargarCaptcha()"><span class="fa fa-refresh"></span></button>
                                <span id="imagen"></span> <br>
                                <input type="text" class="form-control" name="digits">
                            </div>
                          -->
                          <div class="g-recaptcha" data-sitekey="6LdxOAkUAAAAACMacI4-LHtblhSulY5BcRTX_pfE"></div>
                            <button type="submit" class="btn btn-warning btn-lg">Crear</button>
                        </form>

                        <hr>

                        <p>¿Ya tienes una cuenta? <a href="/login">Ingresa aqui</a></p>

            </div>

        </div>
        <script type="text/javascript">
        /*
            function cargarCaptcha() {

                $.ajax({
                    type: "GET",
                    url: '/crearCaptcha',
                    contentType: "image/JPEG",
                    success: function(data) {
                        $('#imagen').html('<img src="data:image/JPEG;base64,' + data + '" />');
                    }
                });
            }

            $(document).ready(function() {
                cargarCaptcha();

            }); */
        </script>
    </main>
    <footer>
        <% include partials/footer %>
    </footer>
</body>

</html>
